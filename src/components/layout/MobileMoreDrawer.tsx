"use client";

import React from "react";

// Mobile navigation now uses the direct 4-tab bottom navigation:
// Home, Prepare, Report, Recovery.
// This drawer is no longer needed in the simplified architecture.
export const MobileMoreDrawer: React.FC = () => null;
