import{z}from"zod";import{AppError}from"./errors";import{assertSafeAiWording}from"@/lib/domain/aiGuard";
const output=z.object({text:z.string().min(1).max(5000),warnings:z.array(z.string()).max(10),sourceFields:z.array(z.string()).max(50)});
export async function explainWithGemini(input:{task:string;verifiedInput:Record<string,unknown>;language:"en"|"fil"}){const key=process.env.GEMINI_API_KEY;const model=process.env.GEMINI_MODEL??"gemini-2.0-flash";if(!key)throw new AppError("GEMINI_UNAVAILABLE","Gemini is not configured. Deterministic output remains available.",503);const prompt=`You are Project AGAP's constrained wording assistant. You may only explain, simplify, translate, or summarize the supplied verified JSON. Never calculate or change risk, invent facts, issue warnings, evacuation orders/routes, safety declarations, relief allocations, or infrastructure decisions. Return JSON only: {"text":string,"warnings":string[],"sourceFields":string[]}. Task: ${input.task}. Language: ${input.language}. Verified input: ${JSON.stringify(input.verifiedInput)}`;try{const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent?key=${encodeURIComponent(key)}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{responseMimeType:"application/json",temperature:0.1}})});if(!response.ok)throw new Error(`Gemini HTTP ${response.status}`);const body=await response.json();const text=body.candidates?.[0]?.content?.parts?.[0]?.text;const parsed=output.parse(JSON.parse(text));assertSafeAiWording(parsed.text);return parsed}catch(error){throw new AppError("GEMINI_UNAVAILABLE","Gemini response failed validation. Use the deterministic template.",503,String(error))}}
