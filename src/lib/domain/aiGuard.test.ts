import{describe,expect,it}from"vitest";import{assertSafeAiWording}from"./aiGuard";describe("AI guard",()=>{it("allows an explanation without modifying decisions",()=>expect(assertSafeAiWording("The verified result requires LGU review.")).toContain("review"));it.each(["Risk is 20","Issue evacuation order","This area is safe","Allocate relief now"])("rejects prohibited output: %s",text=>expect(()=>assertSafeAiWording(text)).toThrow())});
